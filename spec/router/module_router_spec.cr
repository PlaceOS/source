require "../spec_helper"

module PlaceOS::MQTT
  describe Router::Module do
    describe "system_modules" do
      pending "updates on changes to Module name" do
      end
      pending "removes reference on Module destroy"
    end

    describe "drivers" do
      pending "keeps a reference from module_id to driver_id" do
      end
      pending "removes module_id reference on destroy" do
      end
    end
  end
end
