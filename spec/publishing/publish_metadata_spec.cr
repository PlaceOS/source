require "../spec_helper"

module PlaceOS::MQTT
  describe PublishMetadata do
    pending "drops metadata event if model is missing a top-level zone" do
    end
    pending "publishes metadata event if model has a top-level zone" do
    end
  end
end
